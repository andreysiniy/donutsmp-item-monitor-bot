DISCORD_BOT_TOKEN=
DONUT_API_BASE_URL=https://api.donutsmp.net/
DATABASE_URL=sqlite+aiosqlite:///./donutsmp.db
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
TOKEN_ENCRYPTION_KEY=
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
POSTGRES_PASSWORD=

MANIFEST_PATH=manifest_detailed.json
ASSETS_DIR=.

SAFE_REQUESTS_PER_MINUTE=220
RESERVED_REQUESTS_PER_MINUTE=30
DEFAULT_POLL_INTERVAL_SECONDS=3
MAX_SEARCH_PAGES=3
REQUEST_TIMEOUT_SECONDS=10
DEFAULT_HYSTERESIS_PERCENT=2
DEFAULT_NOTIFICATION_COOLDOWN_SECONDS=60
OBSERVATION_RETENTION_DAYS=7
LOG_LEVEL=INFO

